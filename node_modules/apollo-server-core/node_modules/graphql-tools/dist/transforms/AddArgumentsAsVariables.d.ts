import { GraphQLSchema } from 'graphql';
import { Request } from 'apollo-server-core/node_modules/graphql-tools/dist/Interfaces';
import { Transform } from 'apollo-server-core/node_modules/graphql-tools/dist/transforms/transforms';
export default class AddArgumentsAsVariablesTransform implements Transform {
    private schema;
    private args;
    constructor(schema: GraphQLSchema, args: {
        [key: string]: any;
    });
    transformRequest(originalRequest: Request): Request;
}
