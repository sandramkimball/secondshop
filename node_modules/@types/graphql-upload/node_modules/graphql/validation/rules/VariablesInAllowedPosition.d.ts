import { ASTVisitor } from 'graphql-upload/node_modules/graphql/language/visitor';
import { ValidationContext } from 'graphql-upload/node_modules/graphql/validation/ValidationContext';

export function badVarPosMessage(
  varName: string,
  varType: string,
  expectedType: string,
): string;

/**
 * Variables passed to field arguments conform to type
 */
export function VariablesInAllowedPosition(
  context: ValidationContext,
): ASTVisitor;
